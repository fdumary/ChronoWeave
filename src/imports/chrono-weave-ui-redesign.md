Improve the ChronoWeave mobile app UI while keeping the current layout and color identity.

Apply a consistent design system across all screens.

Design goals:
calm sensory interface
dark futuristic wellness product
clean and minimal but elegant

Keep the dark mode theme across every screen. Remove any white backgrounds and convert them to the ChronoWeave dark gradient background.

Background style:
deep indigo gradient
#0B1020 to #141A35 to #1F2950

Create a consistent color system:
Stable time = soft mint #7DD3C7
Moderate drift = lavender #B6A6FF
Fast drift = warm amber #FFC857

Apply these colors consistently across charts, tags, and indicators.

Standardize UI components:
card corner radius 24px
card padding 24px
soft shadow glow
glass effect backgrounds with subtle blur
thin outline borders using #2A3458

Improve spacing and hierarchy:
use consistent vertical spacing between sections
align all cards using a clean grid
reduce crowding on analytics screens

Improve charts:
make the line chart look like a glowing wave representing time rhythm
soft gradient fill under the curve
glowing points on data markers

Improve pie chart:
use softer pastel colors
remove harsh borders
add subtle glow

Improve buttons:
primary button uses teal gradient #38E0C6 to #1FB5A8
secondary buttons use glass style

Improve the bottom navigation bar:
consistent glass blur background
soft glow behind the active icon

Improve onboarding screens:
make the avatar and time graphics slightly larger
add subtle animated glow around the clock illustration

Improve calibration screen:
make the hold button feel more interactive
add ripple pulse animation hint

Ensure the entire UI is responsive using auto layout.
The layout should scale correctly across different device sizes including iPhone SE, iPhone 16, and larger screens.

Use flexible padding and responsive cards so the interface feels balanced on all screen sizes.

Maintain visual consistency across onboarding, dashboard, analytics, and calibration screens so the entire app feels like one cohesive product.